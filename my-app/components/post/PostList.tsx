import { Post } from '@/types/notion';
import PostCard from './PostCard';

type PostListProps = {
  posts: Post[];
};

export default function PostList({ posts }: PostListProps) {
  return null; // TODO: Phase 3에서 구현
}
