import { NotionBlock } from '@/types/notion';

type NotionRendererProps = {
  blocks: NotionBlock[];
};

export default function NotionRenderer({ blocks }: NotionRendererProps) {
  return null; // TODO: Phase 4에서 구현
}
